apiVersion: v1
kind: Service
metadata:
  name: backend-stable
  namespace: smart-asset-manager
spec:
  selector:
    app: backend
  ports:
    - port: 3000
      targetPort: 3000
---
apiVersion: v1
kind: Service
metadata:
  name: backend-canary
  namespace: smart-asset-manager
spec:
  selector:
    app: backend
  ports:
    - port: 3000
      targetPort: 3000
